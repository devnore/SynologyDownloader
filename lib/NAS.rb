# encoding: UTF-8

module NAS
  require_relative './NAS/synology'
  #
  class API
    def initialize(params = {})
      # detect what API we are using and se if it exists, and return it.

    end
  end
end
